merger:
  blockerLabel: /hold
  maxGoroutines: 10
  target_url: http://targeturl
  squashLabel: squash
  policy:
    skipUnknownContexts: false
    required-contexts:
      replace: false
      entries:
        - context3
    required-if-present-contexts:
      replace: true
      entries:
        - context5
        - context6